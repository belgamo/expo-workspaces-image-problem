# expo-workspaces-image-problem
Images from another package/workspace doesn't show up.

# Setup
Just run `yarn` from root directory.

# Running

### Android
  `cd @my-app/expo` and `yarn android`
  
### iOS
  `cd @my-app/expo` and `yarn ios`
